import { Canvas, Meta } from '@storybook/blocks';
import * as Stories from './dataDefinitionList.stories';

<Meta title="SOLID/DataDefinitionList" />

# Data definition list Component

The data definition list is used to display multiple data and numbers.

<Canvas of={Stories.Example} />

## Description

## Usage

`<nk-table-list></nk-table-list>`

## Do

This component takes a slot where a list of `nk-table-item` can be placed in
