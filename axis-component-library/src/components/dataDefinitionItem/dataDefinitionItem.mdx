import { Canvas, Meta } from '@storybook/blocks';
import * as Stories from './dataDefinitionItem.stories';

<Meta title="SOLID/DataDefinitionItem" />

# Data definition item Component

The data definition item is used to display data e.g. display number.

<Canvas of={Stories.Example} />

## Description

## Usage

`<nk-table-item></nk-table-item>`

## @API

### Properties

- variant (`default`, `emphasis-with-border`, `emphasis`, `regular`)
- label (string)
- value (string)
